
迭代过程

现在简化 collapseFakeTokenGaps，去掉策略 2（文本启发式），因为 Silero VAD 足够精确

todo：
cd /Volumes/e61-wrk/develop/aisub && npm run test:engines 2>&1 | grep -A2 "^✗"

npm run test:engines


---
当前.zh.lrc中英文字幕是单行模式，希望双行模式

单行模式如下
[00:01.00]At the market, 在市场上,
[00:03.81]I can see the apples, 我可以看到苹果,

双行模式如下
[00:01.00]At the market
[00:01.00]在市场上
[00:03.81]I can see the apples
[00:03.81]我可以看到苹果,


---
2026-08-05 07:50:08

@Agent 我正在优化 Smartsub v3.6.0 项目，请协助我完成以下三项核心任务：

1. 功能开发：AI翻译配置入口
- 在“AI翻译”配置模块中，新增一个“自定义配置”入口（交互逻辑参考现有的“云端听写”自定义配置）。
- 该自定义配置需支持大模型 API 平台设置，UI 和数据结构请完全参考“深度求索 (DeepSeek)”的配置项。
- 必须包含一个允许用户手动填写/自定义“模型名称”的输入框。

2. 调试环境配置
- 分析当前项目结构，配置好 macOS 下的本地调试环境。
- 确保项目能够成功运行并进入断点调试状态，如有缺失的依赖或环境变量，请自动补全或给出明确指引。

3. 构建环境与版本升级
- 配置好项目的构建 (build) 环境，确保能成功打包应用。
- 将应用的全局版本号从 v3.6.0 升级为 v3.6.1（请检查 package.json、配置文件或构建脚本中的版本字段并统一修改）。

请先分析项目代码结构，制定一个执行计划，确认无误后逐步执行上述任务。每完成一步请向我汇报进度。


---
