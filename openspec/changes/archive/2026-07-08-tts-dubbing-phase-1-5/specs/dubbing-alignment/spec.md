## MODIFIED Requirements

### Requirement: 重叠 cue 检测与顺延

对齐引擎 SHALL 检测时间轴交叠的 cue(前条 end 晚于后条 start)并逐行告警;冲突消解 SHALL 支持两种策略,由配置项(`overlapMode`)选择:

- **顺延(`shift`,默认)**:按 start 顺序顺延后条,顺延产生的时间轴偏移 MUST 反映在槽位规划与可选的顺延字幕输出中(与 v1 行为一致);
- **多轨混合(`mix`)**:重叠行按 start 排序做贪心轨道分配——放入「已放段末端不晚于本行 start」的最小编号轨道,放不下则开新轨;各轨内独立按顺延语义消解残余冲突,轨间互不挤压,使重叠行锚定原始时间轴不被错开。

无重叠的字幕在两种策略下 MUST 产出等价规划(全部行落轨 0)。

#### Scenario: 重叠检测告警

- **WHEN** 两条 cue 时间交叠
- **THEN** 涉及行携带重叠告警标记;顺延模式下拼接结果按 start 顺序顺延不撞车

#### Scenario: 多轨模式重叠行锚定原时间轴

- **WHEN** cue A(00:01.0–00:04.0)与 cue B(00:02.0–00:05.0)时间交叠且 overlapMode = mix
- **THEN** A、B 被分配到不同轨道,规划中两行的目标起点均等于各自原始 start,互不顺延

#### Scenario: 无重叠字幕两策略等价

- **WHEN** 字幕文件无任何时间交叠且 overlapMode = mix
- **THEN** 输出规划与顺延模式完全一致,全部行 lane = 0

### Requirement: 槽位规划输出

对齐引擎 SHALL 输出完整槽位规划(`AlignmentPlan`):每条 cue 的目标起点、目标时长、变速动作(无/预控制 speed 值/atempo 值)、补静音时长、过长/重叠标记、**轨道编号(`lane`,顺延模式恒为 0)**;短于槽位补静音,兜底后仍超长的行为按用户选项「截断」或「顺延」。规划 MUST 支持可选产出「时间轴顺延版字幕」(顺延后的新时间轴;多轨模式下重叠行不因重叠产生顺延)。

#### Scenario: 规划可直接驱动拼接

- **WHEN** 对齐引擎处理完整字幕文件
- **THEN** 输出的规划包含每行的起点/时长/变速/补静音/轨道编号,拼接器无需再做任何时长决策,可按轨道分组直接驱动逐轨拼接

#### Scenario: 单测覆盖轨道分配

- **WHEN** 运行 `test:dubbing`
- **THEN** mix 模式的轨道分配(两两重叠/三行互叠/无重叠回归)与 shift 模式既有用例全部通过
